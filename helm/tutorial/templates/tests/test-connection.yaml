apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tutorial.fullname" . }}-test-connection"
  labels:
    {{- include "tutorial.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tutorial.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
